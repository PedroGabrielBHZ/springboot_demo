spring.application.name=demo

# db stuff
spring.datasource.url=jdbc:mysql://localhost:3306/accounting_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=senhaboa

# springy
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# jwt related
app.jwtSecret=yourSuperSecretKeyThatIsAtLeast256BitsLongAndShouldBeBase64Encoded
app.jwtExpirationMs=86400000

# idk
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# spring sec
spring.security.user.name=pedro
spring.security.user.password=senhaboa

# info
info.app.name=Demo Application
info.app.description=This is a simple Spring Boot demo project.
info.app.version=1.0.0
info.app.author=Pedro Gabriel

# port
server.port=8080